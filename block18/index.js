/*
FUNCTION: Multiplication
EXPECTATIONS: Takes an input of two numbers and returns their product

Multiply (both numbers) {
    if (input is valid)
       return product
    } else {
        return NaN 
    }
}

Edge Cases:
- If user enters the incorrect number of inputs or type of input return NaN
- If user enters a string allow them to be multipled

FUNCTION: Concat Odds
EXPECTATIONS: Takes two arrays and returns an array containing all the odd number in each array, ignoring duplicates

Concat Odd (both arrays) {
    if (input is valid) {
            
        }

    }
Edg Cases: If both inputs are not

*/